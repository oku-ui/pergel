{
  "name": "pergel-core",
  "author": "",
  "license": "ISC",
  "keywords": [],
  "scripts": {
    "build": "pnpm --filter @pergel/cli build && pnpm --filter pergel build",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check": "pnpm typecheck"
  },
  "dependencies": {
    "bumpp": "^9.4.0"
  },
  "devDependencies": {
    "unbuild": "^2.0.0"
  },
  "resolutions": {
    "bumpp": "9.4.0"
  }
}
